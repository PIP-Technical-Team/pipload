% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_dlw_data.R
\name{load_dlw_data}
\alias{load_dlw_data}
\alias{find_dlw_data}
\alias{check_dlw_id_name}
\alias{load_dlw_gmd_inventory}
\alias{load_dlw_gmd_log}
\alias{load_gmd_valid_inv}
\alias{load_gmd_valid_log}
\alias{load_gmd_valid_report}
\title{Load and find data from dlw repository}
\usage{
load_dlw_data(
  country_code = NULL,
  surveyid_year = NULL,
  survey_acronym = NULL,
  vermast = NULL,
  veralt = NULL,
  collection = "GMD",
  module = "GPWG",
  latest_version = TRUE,
  latest_year = FALSE,
  id_name = NULL,
  version = NULL,
  format = "qs2",
  verbose = getOption("pipload.verbose")
)

find_dlw_data(
  dir = NULL,
  latest_version = TRUE,
  latest_year = FALSE,
  verbose = getOption("pipload.verbose"),
  ...
)

check_dlw_id_name(id_name)

load_dlw_gmd_inventory()

load_dlw_gmd_log()

load_gmd_valid_inv()

load_gmd_valid_log()

load_gmd_valid_report()
}
\arguments{
\item{country_code}{Character: country ISO 3 code.}

\item{vermast}{character: Version of the master data in the form "vXX" where
X is a number of two digits like "01" or "02".}

\item{veralt}{character: Version of the alternative  data in the form "vXX"
where X is a number of two digits like "01" or "02".}

\item{collection}{character: It should always be "GMD"}

\item{module}{character: module of GMD collection (e.g., ALL, GPWG, L).
Default is GPWG}

\item{latest_version}{logical: If TRUE and  \code{vermast} and \code{veralt} are NULL,
it will use the most recent version of the data for a particular year.}

\item{latest_year}{logical: If \code{TRUE} and  \code{year} is NULL, it retrieves the
most recent year. Otherwise, it will return the calls for all the years
available. This is the default.}

\item{id_name}{id name of dlw data}

\item{version}{An integer or a quoted directive. Retrieve a specific version
of an artifact. See details in the function body for supported directives.}

\item{verbose}{logical. If TRUE display information. Default is option
"pipload.verbose"}

\item{dir}{dir from \code{pipfun::get_pip_folders(folder = "dlw_data")}}

\item{...}{Just the following: country_code, year, module, survey, vermast,
veralt, collection, module. They work exactly the same as the ones in
\link{load_dlw_data}}

\item{year}{numeric: four digit year}

\item{survey}{character: survey acronyn}

\item{filename}{character: File name}
}
\value{
data table with dlw data

data from with filter data

character with id_name

\link{load_dlw_gmd_inventory} data.table with inventory of GMD data from
DLW

\link{load_dlw_gmd_log} data.table with log of retrieval process from DLW

\link{load_gmd_valid_inv} data.table with inventory of validated GMD data

\link{load_gmd_valid_log} data.table with log of GMD validated workflow

\link{load_gmd_valid_report} data.table with validation report data
}
\description{
\link{load_dlw_data} loads data from dlw. \link{find_dlw_data} Find data available in
DLW data board. \link{load_dlw_gmd_inventory} loads inventory of GMD data from
DLW. \link{load_dlw_gmd_log} loads GMD log of GMD from DLW. \link{load_gmd_valid_inv}
loads GMD validated inventory. \link{load_gmd_valid_log} loads GMD validation
workflow log. \link{load_gmd_valid_report} loads GMD validation report.
}
\examples{
\dontrun{
lr <- pipfun::get_latest_pip_release()
pipfun::setup_working_release(release = lr$release,
                              identity = lr$identity,
                              verbose = FALSE)

# Using id_name
load_dlw_data(id_name = "HRV_2011_EU-SILC_V01_M_V04_A_GMD_GPWG.qs")

# without ext also works
load_dlw_data(id_name = "HRV_2011_EU-SILC_V01_M_V04_A_GMD_GPWG")

# country and year
load_dlw_data(country_code = "HRV", year = 2011)

# Find data
find_dlw_data(country_code = "HRV")

# Latest year in EACH module
find_dlw_data(country_code = "HRV", latest_year = TRUE)
}
check_dlw_id_name("HRV_2011_EU-SILC_V01_M_V04_A_GMD_GPWG.qs")
check_dlw_id_name("HRV_2011_EU-SILC_V01_M_V04_A_GMD_GPWG")
load_dlw_gmd_inventory()
load_dlw_gmd_log()
load_gmd_valid_inv()
load_gmd_valid_log()
load_gmd_valid_report()
}
\keyword{internal}
