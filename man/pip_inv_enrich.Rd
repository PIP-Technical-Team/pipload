% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_inv_enrich.R
\name{pip_inv_enrich}
\alias{pip_inv_enrich}
\title{Enrich a PIP inventory with fields from per-survey metadata}
\usage{
pip_inv_enrich(inv, fields = character(0))
}
\arguments{
\item{inv}{A \code{data.table} containing the PIP master inventory. Must have a
\code{pip_id} column. If a \code{version_id_metadata} column is present, it is used
to load the exact metadata version for each survey.}

\item{fields}{Character vector naming list elements to extract from each
survey's metadata object (e.g. \code{"reporting_level"}). Each named element
is added as a column in the returned inventory. Surveys whose metadata
cannot be loaded receive \code{NA} for all requested fields.}
}
\value{
The input \code{inv} with the requested columns added or replaced.
Pre-existing columns matching any requested field name (including joyn
suffix variants like \code{field.x} / \code{field.y}) are removed before the new
values are joined in.
}
\description{
For each \code{pip_id} in \code{inv}, loads the corresponding metadata artifact from
stamp (the \code{"pip_meta"} alias) and extracts the requested fields as new
columns in the inventory. The exact metadata version is resolved from
\code{inv$version_id_metadata} when present; otherwise the latest version is
used.
}
\details{
This function is called automatically by \code{\link[=load_pip_master_inventory]{load_pip_master_inventory()}} when
its \code{fields} argument is non-empty. By default no enrichment is performed.
Usage example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{inv <- load_pip_master_inventory(fields = "reporting_level")
# Or enrich after loading:
raw_inv <- load_pip_master_inventory()
enriched <- pip_inv_enrich(raw_inv, fields = "reporting_level")
}\if{html}{\out{</div>}}

Metadata is loaded via \code{\link[=pip_read]{pip_read()}} with \code{alias = "pip_meta"}. Loading
errors (e.g. stale version, missing artifact) are caught silently — the
affected rows receive \code{NA} for all requested fields and a warning is emitted
listing the affected \code{pip_id}s.
}
\examples{
\dontrun{
inv <- load_pip_master_inventory(fields = "reporting_level")
# Or enrich multiple fields:
inv2 <- pip_inv_enrich(inv, fields = c("reporting_level", "welfare_type"))
}
}
\seealso{
\code{\link[=load_pip_master_inventory]{load_pip_master_inventory()}}
}
\concept{inventory}
