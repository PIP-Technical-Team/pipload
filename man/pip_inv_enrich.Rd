% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_inv_enrich.R
\name{pip_inv_enrich}
\alias{pip_inv_enrich}
\title{Enrich a PIP inventory with fields from per-survey metadata}
\usage{
pip_inv_enrich(inv, fields = character(0))
}
\arguments{
\item{inv}{A \code{data.table} containing the PIP inventory. Must have \code{pip_id}
and \code{path_metadata} columns. The \code{path_metadata} column is present in
inventories loaded via \code{\link[=load_pip_release_inventory]{load_pip_release_inventory()}} or
\code{\link[=load_pip_master_inventory]{load_pip_master_inventory()}}. If it is absent, the function aborts with
an informative error.

If a \code{version_id_metadata} column is present, the exact versioned artifact
is read (\verb{path_metadata/versions/<version_id>/artifact}). Rows where
\code{version_id_metadata} is \code{NA} have no saved metadata and receive \code{NA} for
all requested fields.}

\item{fields}{Character vector of metadata field names to add as columns.
Fields already present as columns in \code{inv} are skipped with an
informational message (no overwrite). Must be a subset of the following
valid fields:

\strong{Scalar fields} (add one column each):
\code{"gd_type"}, \code{"reporting_level"}, \code{"ppp_data_level"}, \code{"cpi_data_level"},
\code{"gdp_data_level"}, \code{"pce_data_level"}, \code{"pop_data_level"},
\code{"aux_data_levels"}, \code{"distribution_type"}

\strong{Vector fields} (expand into multiple wide columns):
\itemize{
\item \code{"cpi"} — columns named \verb{cpi_<YYYY>_<area>} (e.g. \code{cpi_2005_rural})
\item \code{"ppp"} — columns use existing names as-is (e.g. \code{ppp_2011_02_02_national})
\item \code{"pop"}, \code{"gdp"}, \code{"pce"} — columns named \verb{<field>_<area>} when the
embedded year matches \code{surveyid_year} (e.g. \code{pop_rural}); when the year
differs, the full element name is kept (\code{pop_2019_national}) and a
\verb{<field>_year} column is added.
}}
}
\value{
A \code{data.table} identical to the input \code{inv} with the requested
columns appended. The number of rows is unchanged. Vector fields may add many columns (up to one per
unique area × year combination across all surveys); surveys lacking a
particular element receive \code{NA}.
}
\description{
For each \code{pip_id} in \code{inv}, loads the corresponding metadata artifact
directly from its versioned path on disk (bypassing stamp version
resolution) and expands the requested fields as new columns. Only fields
in the whitelist are accepted; see \code{fields} below.
}
\details{
This function is called automatically by \code{\link[=load_pip_release_inventory]{load_pip_release_inventory()}} and
\code{\link[=load_pip_master_inventory]{load_pip_master_inventory()}} when their \code{fields} argument is non-empty.
By default no enrichment is performed.

\strong{Fast path}: metadata is read directly via \code{qs2::qs_read()} using the
versioned file path from the inventory, avoiding stamp's per-artifact
version resolution overhead (~5ms/file vs ~90ms/file).

\strong{No overwrite}: fields found as existing columns in \code{inv} are skipped
with an informational message. To force refresh, remove the columns first.
}
\note{
When \code{version_id_metadata} is \code{NA} for a survey, it means no metadata
artifact was saved for it during the pipeline run. This is unexpected and
should be investigated — check the pipeline output for that \code{pip_id} to
understand why it was not produced.
}
\examples{
\dontrun{
inv <- load_pip_master_inventory(fields = "reporting_level")

# Enrich after loading with a vector field:
raw_inv <- load_pip_master_inventory()
enriched <- pip_inv_enrich(raw_inv, fields = c("reporting_level", "cpi"))
# Results in columns: reporting_level, cpi_2005_rural, cpi_2011_urban, ...
}
}
\seealso{
\code{\link[=load_pip_release_inventory]{load_pip_release_inventory()}}, \code{\link[=load_pip_master_inventory]{load_pip_master_inventory()}}
}
\concept{inventory}
