% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_pip_deflated_data.R
\name{load_pip_deflated_data}
\alias{load_pip_deflated_data}
\title{Load and deflate a PIP survey in a single call}
\usage{
load_pip_deflated_data(
  country_code = NULL,
  surveyid_year = NULL,
  survey_acronym = NULL,
  welfare_type = NULL,
  module = NULL,
  id_name = NULL,
  vermast = NULL,
  veralt = NULL,
  collection = NULL,
  latest_version = TRUE,
  latest_year = FALSE,
  where = c("release", "master"),
  version = NULL,
  verbose = getOption("pipload.verbose"),
  format = "qs2",
  cpi = NULL,
  ppp = NULL,
  pop = NULL
)
}
\arguments{
\item{country_code}{Character: country ISO 3 code.}

\item{surveyid_year}{numeric: four digit year}

\item{survey_acronym}{character: survey acronym}

\item{welfare_type}{character: either "INC" for income or "CON" for
consumption. This is only useul when there are two welfare types in the
same year for the same country}

\item{module}{character: module of GMD collection (e.g., ALL, GPWG, L).
Default is GPWG}

\item{id_name}{id name of pip data}

\item{vermast}{character: Version of the master data in the form "vXX" where
X is a number of two digits like "01" or "02".}

\item{veralt}{character: Version of the alternative  data in the form "vXX"
where X is a number of two digits like "01" or "02".}

\item{collection}{character: It should always be "GMD"}

\item{latest_version}{logical: If TRUE and  \code{vermast} and \code{veralt} are NULL,
it will use the most recent version of the data given by stamp.}

\item{latest_year}{logical: If \code{TRUE} and  \code{year} is NULL, it retrieves the
most recent year. Otherwise, it will return the calls for all the years
available. This is the default.}

\item{where}{character: Either "release" or "master". Se details.}

\item{version}{character: Specific data version to load; forwarded to \code{pip_read}. Default is \code{NULL} (latest if available).}

\item{verbose}{logical. If TRUE display information. Default is option
"pipload.verbose"}

\item{format}{character: Data format to read (for example, \code{"qs2"}). Default is \code{"qs2"}.}

\item{cpi}{data.table or NULL. CPI table to override the default resolved
by \code{pd_deflation()}. Passed directly to \code{pipdata::pd_deflation(cpi = )}.}

\item{ppp}{data.table or NULL. PPP table to override the default. Passed
directly to \code{pipdata::pd_deflation(ppp = )}.}

\item{pop}{data.table or NULL. Population table to override the default.
Passed directly to \code{pipdata::pd_deflation(pop = )}.}
}
\value{
A data.table — the deflated survey returned by
\code{pipdata::pd_deflation()}.
}
\description{
Convenience wrapper that chains \code{\link[=load_pip_data]{load_pip_data()}} and
\code{pipdata::pd_deflation()}. All filtering arguments are forwarded to
\code{\link[=load_pip_data]{load_pip_data()}}; the resulting survey data.table is then passed to
\code{pipdata::pd_deflation()}.
}
\details{
\code{pipdata} is a \strong{soft dependency} (\code{Suggests}). The function will abort
with an informative error if \code{pipdata} is not installed. Do not add
\code{pipdata} to \code{Imports} — that would create a circular dependency since
\code{pipdata} imports \code{pipload}.
}
\examples{
\dontrun{
lr <- pipfun::get_latest_pip_release()
pipfun::setup_working_release(
  release  = lr$release,
  identity = lr$identity,
  verbose  = FALSE
)

# Using id_name
load_pip_deflated_data(id_name = "BOL_2022_EH_INC_ALL")

# Using country + year filters
load_pip_deflated_data(country_code = "PRY", surveyid_year = 2018)
}
}
\seealso{
\code{\link[=load_pip_data]{load_pip_data()}}
}
\concept{load_pip_data}
