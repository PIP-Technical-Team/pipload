% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_pip_data.R
\name{load_pip_data}
\alias{load_pip_data}
\alias{find_pip_data}
\alias{load_pip_release_inventory}
\alias{load_pip_master_inventory}
\alias{check_pip_id_name}
\title{Set of functions to interact with PIP data.}
\usage{
load_pip_data(
  country_code = NULL,
  surveyid_year = NULL,
  survey_acronym = NULL,
  welfare_type = NULL,
  module = NULL,
  id_name = NULL,
  vermast = NULL,
  veralt = NULL,
  collection = NULL,
  latest_version = TRUE,
  latest_year = FALSE,
  where = c("release", "master"),
  version = NULL,
  verbose = getOption("pipload.verbose"),
  format = "qs2",
  metadata = FALSE
)

find_pip_data(
  latest_year = FALSE,
  where = c("release", "master"),
  verbose = getOption("pipload.verbose"),
  latest_version = FALSE,
  vermast = NULL,
  veralt = NULL,
  collection = NULL,
  ...
)

load_pip_release_inventory(
  version = NULL,
  verbose = getOption("pipload.verbose"),
  format = "qs2",
  fields = character(0)
)

load_pip_master_inventory(
  format = "qs2",
  version = NULL,
  verbose = getOption("pipload.verbose"),
  fields = character(0)
)

check_pip_id_name(id_name)
}
\arguments{
\item{country_code}{Character: country ISO 3 code.}

\item{surveyid_year}{numeric: four digit year}

\item{survey_acronym}{character: survey acronym}

\item{welfare_type}{character: either "INC" for income or "CON" for
consumption. This is only useul when there are two welfare types in the
same year for the same country}

\item{module}{character: module of GMD collection (e.g., ALL, GPWG, L).
Default is GPWG}

\item{id_name}{id name of pip data}

\item{vermast}{character: Version of the master data in the form "vXX" where
X is a number of two digits like "01" or "02".}

\item{veralt}{character: Version of the alternative  data in the form "vXX"
where X is a number of two digits like "01" or "02".}

\item{collection}{character: It should always be "GMD"}

\item{latest_version}{logical: If TRUE and  \code{vermast} and \code{veralt} are NULL,
it will use the most recent version of the data given by stamp.}

\item{latest_year}{logical: If \code{TRUE} and  \code{year} is NULL, it retrieves the
most recent year. Otherwise, it will return the calls for all the years
available. This is the default.}

\item{where}{character: Either "release" or "master". Se details.}

\item{version}{character: Specific data version to load; forwarded to \code{pip_read}. Default is \code{NULL} (latest if available).}

\item{verbose}{logical. If TRUE display information. Default is option
"pipload.verbose"}

\item{format}{character: Data format to read (for example, \code{"qs2"}). Default is \code{"qs2"}.}

\item{metadata}{logical: If TRUE, it will load metadata instead of data. Default is FALSE}

\item{...}{Just the following: country_code, year, survey, welfare_type,and
module. They work exactly the same as the ones in \link{load_pip_data}}

\item{fields}{Character vector of metadata field names to extract from each
survey's stored metadata artifact and add as columns. Passed to
\code{\link[=pip_inv_enrich]{pip_inv_enrich()}}. Default is \code{character(0)} (no enrichment). Example:
\code{fields = "reporting_level"} adds the reporting level from each survey's
metadata.}
}
\value{
data.table with pip data. Note: one of \code{country_code} or \code{id_name} must be provided.

filtered data table from inventory

data.table with PIP inventory for the current release

data.table with PIP master inventory

character with id_name
}
\description{
\link{load_pip_data} Load data from pip repository. \link{find_pip_data} Finds data
available. \link{load_pip_release_inventory} Loads pip inventory for
corresponding release. \link{load_pip_master_inventory} Loads pip master inventory
for corresponding release. \link{check_pip_id_name} checks that pip id_name is
correct (INTERNAL)
}
\details{
\strong{where} specifies \emph{where} to find data. It could be at the release level or at the master level. Release refers to the surveys specified in the Price Framework data of each release. Master refers to all the pip cleaned data
}
\examples{
\dontrun{
lr <- pipfun::get_latest_pip_release()
pipfun::setup_working_release(release = lr$release,
                              identity = lr$identity,
                              verbose = FALSE)

# Using id_name
load_pip_data(id_name = "BOL_2022_EH_INC_ALL")
}
\dontrun{
lr <- pipfun::get_latest_pip_release()
pipfun::setup_working_release(release = lr$release,
                              identity = lr$identity,
                              verbose = FALSE)

# Find data
find_pip_data(country_code = "BOL")

# Latest year in EACH module
find_pip_data(country_code = "BOL", latest_year = TRUE)
}
\dontrun{
lr <- pipfun::get_latest_pip_release()
pipfun::setup_working_release(release = lr$release,
                              identity = lr$identity,
                              verbose = FALSE)
load_pip_release_inventory()
}
\dontrun{
lr <- pipfun::get_latest_pip_release()
pipfun::setup_working_release(release = lr$release,
                              identity = lr$identity,
                              verbose = FALSE)
load_pip_master_inventory()
}
\dontrun{
check_pip_id_name("AGO_2000_HBS_CON_GPWG.qs2")
check_pip_id_name("AGO_2000_HBS_CON_GPWG")
}
}
\keyword{internal}
