% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_load_all_aux.R
\name{pip_load_all_aux}
\alias{pip_load_all_aux}
\title{Load all auxiliary files in memory}
\usage{
pip_load_all_aux(
  aux = c("cpi", "ppp", "pfw", "pop", "gdm"),
  aux_names = aux,
  envir = globalenv(),
  root_dir = Sys.getenv("PIP_ROOT_DIR"),
  maindir = pip_create_globals(root_dir)$PIP_DATA_DIR,
  version = NULL,
  file_to_load = NULL,
  apply_label = TRUE,
  verbose = FALSE,
  preferred_format = NULL
)
}
\arguments{
\item{aux}{character: auxiliary files to load. Default is c("cpi", "ppp",
"pfw", "pop", "gdm", "gdp", "pce")}

\item{aux_names}{character of the same length of \code{aux}. Names of objects to
be loaded. default is \code{aux}}

\item{envir}{environment where the data frame will be allocated. Default is
\code{globalenv()}}

\item{root_dir}{character: root directory of the PIP data}

\item{version}{An integer or a quoted directive. "available": displays list of
available versions for \code{measure}. "select"|"pick"|"choose": allows user to
select the vintage of \code{measure}. if the integer is a negative number
(e.g., \code{-1}), \code{pip_load_aux} will load that number of versions before the
most recent version available. So, if \code{-1} it will load the version before the
current, \code{-2} loads two versions before the current one, and so on. If it is
a positive number, it must be quoted and in the form "\%Y\%m\%d\%H\%M\%S".}

\item{file_to_load}{character: file path to load. Does not work with any other
argument}

\item{apply_label}{logical: if TRUE, predefined labels will apply to data
loaded using \code{file_to_load} argument. Default TRUE. Tip: change to FALSE if
the main structure of data has changed and labels have not been updated}

\item{verbose}{logical: whether to display message. Default is TRUE}

\item{preferred_format}{character: preferred format. default is "fst".}
}
\value{
invisible TRUE
}
\description{
Load all auxiliary files in memory
}
