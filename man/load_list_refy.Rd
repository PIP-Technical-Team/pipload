% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_load_lineups.R
\name{load_list_refy}
\alias{load_list_refy}
\title{Load lineup distributions as a list for given country-years}
\usage{
load_list_refy(input_list, path = Sys.getenv("PIP_LINEUPS_DIR"))
}
\arguments{
\item{input_list}{A list with two elements: \code{country_code} and \code{year}.
\itemize{
\item \code{country_code}: A vector of country codes (e.g., "ZAF", "COL").
\item \code{year}: Either a single value (numeric or string) representing a year to be applied to all country codes, or a list of years where each element corresponds to a country code. If \code{year} is provided as a list, its length must match the length of \code{country_code}.
}}

\item{path}{A string representing the directory path where the \code{.qs} files are stored.}
}
\value{
A list containing the appended lineup data for all specified countries and years. The attributes of each loaded dataset are stored as additional attributes in the returned data table.
}
\description{
This function loads multiple data frames as a list containing lineup distribution data for specific countries and years.
It also stores the attributes of each loaded dataset in the returned data frame.
}
\examples{
\dontrun{
input_list <- list(
  country_code = c("ZAF", "COL"),
  year = list(c(2020, 2021), c(2015, 2016))
)
all_lineups <- load_append_refy(input_list)
}
}
