% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_load_lineups.R
\name{transform_input}
\alias{transform_input}
\title{Transform Input List for Country-Year Combinations}
\usage{
transform_input(input_list)
}
\arguments{
\item{input_list}{A list with two elements: \code{country_code} and \code{year}.
\itemize{
\item \code{country_code}: A vector of country codes (e.g., "ZAF", "COL").
\item \code{year}: Either a single value (numeric or string) representing a year to be applied to all country codes, or a list of years where each element corresponds to a country code. If \code{year} is provided as a list, its length must match the length of \code{country_code}.
}}
}
\value{
A list where each element is a list containing \code{country_code} and \code{year}, representing all appropriate combinations of the input \code{country_code} and \code{year}.
}
\description{
Transform Input List for Country-Year Combinations
}
\examples{
\dontrun{
# Example with a single year for all countries
input_list <- list(
  country_code = c("ZAF", "COL"),
  year = 2020
)
transformed_list <- transform_input(input_list)

# Example with different years for each country
input_list <- list(
  country_code = c("ZAF", "COL"),
  year = list(c(2020, 2021), c(2015, 2016))
)
transformed_list <- transform_input(input_list)
}
}
